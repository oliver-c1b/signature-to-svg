{
  "name": "signature-to-svg",
  "version": "1.0.0",
  "private": true,
  "description": "Private, offline bitmap signature tracing in the browser.",
  "type": "module",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:pages": "vite build --outDir docs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test"
  },
  "dependencies": {
    "esm-potrace-wasm": "0.5.0"
  },
  "devDependencies": {
    "vite": "8.1.4",
    "vite-plugin-singlefile": "2.3.3"
  }
}
